---
phase: 03-learning-outcomes
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - components/dashboard/po-low-grade-panel.tsx
  - app/(dashboard)/learning-outcomes/page.tsx
autonomous: true
requirements: [LO-03, LO-04]

must_haves:
  truths:
    - "교수가 학습성과 탭(LO2/LO5/LO3)을 선택하면 해당 PO의 하 등급 학생 목록이 익명 처리된 상태로 표시된다"
    - "교수가 하 등급 학생을 클릭하면 사전학습·보고서·실습교수·현장지도자·해당 학습성과 세부점수가 표시된다"
    - "하 등급 학생이 0명인 PO를 선택하면 '하 등급 학생 없음' 빈 상태 메시지가 표시된다"
    - "반 필터(전체/A/B/C)로 특정 반의 하 등급 학생만 볼 수 있다"
  artifacts:
    - path: "components/dashboard/po-low-grade-panel.tsx"
      provides: "하 등급 학생 드릴다운 Client Component"
      exports: ["PoLowGradePanel"]
      contains: "use client"
    - path: "app/(dashboard)/learning-outcomes/page.tsx"
      provides: "드릴다운 패널이 통합된 학습성과 페이지"
      contains: "PoLowGradePanel"
  key_links:
    - from: "components/dashboard/po-low-grade-panel.tsx"
      to: "lib/grade-po.ts"
      via: "getLowGradeStudents() import"
      pattern: "import.*getLowGradeStudents.*grade-po"
    - from: "app/(dashboard)/learning-outcomes/page.tsx"
      to: "components/dashboard/po-low-grade-panel.tsx"
      via: "PoLowGradePanel 렌더링 with students props"
      pattern: "<PoLowGradePanel.*students="
---

<objective>
하 등급 학생 드릴다운 패널(Client Component)을 구현하여 교수가 학습성과별 하 등급 학생을 조회하고 세부 점수를 확인할 수 있도록 한다.

Purpose: LO-03(하 등급 학생 익명 목록 조회)과 LO-04(하 등급 학생 세부점수 드릴다운)를 완성하여 인증평가 핵심 기능을 완성한다.
Output: po-low-grade-panel.tsx (Client Component), 통합된 learning-outcomes/page.tsx
</objective>

<execution_context>
@C:/Users/김기훈.LAPTOP-1R73PMSJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/김기훈.LAPTOP-1R73PMSJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-learning-outcomes/03-RESEARCH.md

# Plan 01 산출물 (직접 의존)
@.planning/phases/03-learning-outcomes/03-01-SUMMARY.md
@lib/grade-po.ts
@app/(dashboard)/learning-outcomes/page.tsx

# 타입 정의
@types/grades.ts

# 기존 Client Component 패턴 참조
@components/dashboard/grade-histogram-chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: 하 등급 학생 드릴다운 Client Component 구현</name>
  <files>components/dashboard/po-low-grade-panel.tsx</files>
  <action>
components/dashboard/po-low-grade-panel.tsx 신규 파일을 생성한다. 03-RESEARCH.md Pattern 5를 기반으로 "use client" Client Component로 구현한다.

1. "use client" 지시자를 파일 최상단에 배치

2. Props 인터페이스:
   - `PoLowGradePanelProps { students: StudentGrade[] }` -- 전체 학생 배열을 Server Component에서 props로 전달받음

3. 상태 관리 (useState 3개):
   - `selectedPo: PoKey` (기본값 'po2')
   - `selectedClass: ClassCode | 'all'` (기본값 'all') -- 반 필터
   - `selectedStudent: StudentGrade | null` (기본값 null)

4. PO 탭 UI:
   - PO_TABS 상수: [{ key: 'po2', label: 'LO2 대상자간호' }, { key: 'po5', label: 'LO5 안전과질' }, { key: 'po3', label: 'LO3 전문직' }]
   - Tailwind button 스타일링 (Radix Tabs 사용 안 함)
   - 선택됨: bg-primary text-primary-foreground
   - 비선택: bg-muted text-muted-foreground hover:bg-muted/80
   - 탭 변경 시 selectedStudent = null 초기화, selectedClass = 'all' 초기화

5. 반 필터 UI:
   - CLASS_FILTERS: [{ key: 'all', label: '전체' }, { key: 'A', label: 'A반' }, { key: 'B', label: 'B반' }, { key: 'C', label: 'C반' }]
   - PO 탭 아래에 별도 행으로 배치
   - 반 변경 시 selectedStudent = null 초기화

6. 하 등급 학생 목록:
   - getLowGradeStudents(students, selectedPo, selectedClass === 'all' ? undefined : selectedClass) 호출
   - 각 학생을 button으로 렌더링: 이름(익명화됨) + 반 + 해당PO 점수
   - 선택된 학생: bg-accent text-accent-foreground
   - 비선택: hover:bg-muted
   - 목록 상단에 "하 등급 학생 N명" 카운트 표시
   - 빈 상태: "하 등급 학생 없음" 메시지 (text-muted-foreground, 중앙 정렬)

7. 선택된 학생 세부점수 패널:
   - selectedStudent가 null이 아닐 때만 표시
   - border rounded-lg p-4 래퍼
   - 이름 표시 (익명화 상태 그대로)
   - dl > div.flex.justify-between 패턴으로 5개 항목 나열:
     - 사전학습: {selectedStudent.preLearning}점 / 10점
     - 보고서: {selectedStudent.reportTotal}점 / 30점
     - 실습지도교수: {selectedStudent.profTotal}점 / 20점
     - 현장지도자: {selectedStudent.fieldTotal}점 / 20점
     - [해당 PO 이름] 점수: {selectedStudent[selectedPo]}점 (빨강 강조, border-t로 구분)
   - PO 이름 동적 매핑: po2 -> 'LO2 대상자간호', po5 -> 'LO5 안전과질', po3 -> 'LO3 전문직'

8. 전체 레이아웃:
   - Card > CardHeader("하 등급 학생 드릴다운") > CardContent
   - PO 탭 → 반 필터 → grid grid-cols-1 md:grid-cols-2 gap-4 (좌: 목록, 우: 세부)

Imports:
- useState from 'react'
- Card, CardContent, CardHeader, CardTitle from '@/components/ui/card'
- StudentGrade from '@/types/grades' (import type)
- PoKey from '@/lib/grade-po' (import type)
- getLowGradeStudents from '@/lib/grade-po'

주의사항:
- getGradeData()를 Client Component에서 직접 호출하지 말 것 (googleapis 서버 전용)
- getLowGradeStudents는 순수 TypeScript 함수이므로 Client에서 import 가능
- PO_THRESHOLDS 하드코딩 금지 -- getLowGradeStudents가 내부적으로 처리
- 학생 key는 studentId 사용 (익명화되어 있지만 유니크)
  </action>
  <verify>npx tsc --noEmit 컴파일 에러 없음. 파일 최상단에 "use client" 존재. PoLowGradePanel이 named export됨.</verify>
  <done>PoLowGradePanel Client Component가 PO 탭 선택, 반 필터, 하 등급 학생 목록, 학생 선택 시 세부점수 표시 기능을 모두 포함하고 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 학습성과 페이지에 드릴다운 패널 통합</name>
  <files>app/(dashboard)/learning-outcomes/page.tsx</files>
  <action>
03-01에서 생성된 learning-outcomes/page.tsx에 PoLowGradePanel을 추가한다.

1. import 추가:
   - `import { PoLowGradePanel } from '@/components/dashboard/po-low-grade-panel'`

2. JSX에서 PoAchievementTable 아래에 PoLowGradePanel 추가:
   ```
   <PoAchievementTable poData={poData} />
   <PoLowGradePanel students={students} />
   ```
   - students는 이미 getGradeData()에서 가져온 전체 학생 배열

3. Plan 01에서 남겨둔 "Plan 03-02에서 PoLowGradePanel 추가 예정" 주석 제거

주의사항:
- students를 Client Component에 props로 전달할 때 StudentGrade[]는 plain object 배열이므로 직렬화 가능 -- 함수나 Map 포함 없음
- page.tsx는 Server Component 유지 (async function, "use client" 없음)
  </action>
  <verify>npm run build 성공. 브라우저에서 /learning-outcomes 접속 시 분포표 아래에 드릴다운 패널이 렌더링됨. PO 탭 클릭 시 하 등급 학생 목록이 변경됨. 학생 클릭 시 세부점수 표시됨.</verify>
  <done>학습성과 페이지에서 분포표와 드릴다운 패널이 함께 표시되며, PO 탭 선택 → 반 필터 → 학생 선택 → 세부점수 조회 전체 플로우가 작동한다.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- 타입 에러 없음
2. `npm run build` -- 프로덕션 빌드 성공
3. /learning-outcomes 페이지에서:
   a. LO2 탭 클릭 → 하 등급 학생 목록 표시 (이름 익명화 확인: "김*우" 형태)
   b. LO5 탭 클릭 → 해당 PO 하 등급 학생으로 목록 갱신
   c. 반 필터 "A반" 클릭 → A반 학생만 표시
   d. 학생 클릭 → 우측에 사전학습/보고서/실습교수/현장지도자/해당PO점수 5개 항목 표시
   e. 하 등급 학생 0명인 PO 선택 시 빈 상태 메시지 표시
4. 모든 학생 이름이 익명화된 상태("김*우")로만 표시됨
</verification>

<success_criteria>
- LO-03: 학습성과별 하 등급 학생 목록이 익명 처리된 상태로 조회된다
- LO-04: 하 등급 학생 선택 시 사전학습/보고서/실습교수/현장지도자/해당PO 세부점수가 표시된다
- 반 필터(전체/A/B/C)로 특정 반 학생만 볼 수 있다
- 빈 상태 처리 정상 작동
- 빌드 에러 없음
</success_criteria>

<output>
After completion, create `.planning/phases/03-learning-outcomes/03-02-SUMMARY.md`
</output>
