---
phase: 04-student-lookup-class-comparison
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/grade-comparison.ts
  - components/dashboard/class-comparison-chart.tsx
  - components/dashboard/nursing-skills-stats.tsx
  - app/(dashboard)/class-comparison/page.tsx
  - components/layout/sidebar.tsx
autonomous: true
requirements: [COMP-01, COMP-02]

must_haves:
  truths:
    - "교수가 반별 비교 페이지에서 A/B/C반의 평균, 최고점, 최저점을 나란히 비교하는 BarChart를 확인할 수 있다"
    - "교수가 핵심간호술(100점 만점) 통계를 총점과 분리된 별도 섹션에서 반별+전체로 확인할 수 있다"
    - "사이드바에 반별 비교 링크가 표시되고 클릭하면 /class-comparison 페이지로 이동한다"
    - "핵심간호술 섹션 헤더에 총점 미포함임이 명시되어 있다"
  artifacts:
    - path: "lib/grade-comparison.ts"
      provides: "반별 비교 + 핵심간호술 집계 유틸"
      exports: ["buildClassComparisonData", "computeNursingSkillsStats", "ClassComparisonData", "NursingSkillsStats"]
    - path: "components/dashboard/class-comparison-chart.tsx"
      provides: "반별 비교 grouped BarChart Client Component"
      contains: "use client"
    - path: "components/dashboard/nursing-skills-stats.tsx"
      provides: "핵심간호술 반별+전체 KPI 카드 컴포넌트"
      contains: "NursingSkillsStatsCard"
    - path: "app/(dashboard)/class-comparison/page.tsx"
      provides: "/class-comparison 라우트 Server Component"
      exports: ["default"]
  key_links:
    - from: "app/(dashboard)/class-comparison/page.tsx"
      to: "lib/grade-data.ts"
      via: "getGradeData() 호출"
      pattern: "getGradeData"
    - from: "app/(dashboard)/class-comparison/page.tsx"
      to: "lib/grade-comparison.ts"
      via: "buildClassComparisonData(), computeNursingSkillsStats() 호출"
      pattern: "buildClassComparisonData|computeNursingSkillsStats"
    - from: "app/(dashboard)/class-comparison/page.tsx"
      to: "components/dashboard/class-comparison-chart.tsx"
      via: "data props 전달"
      pattern: "ClassComparisonChart.*data"
    - from: "app/(dashboard)/class-comparison/page.tsx"
      to: "components/dashboard/nursing-skills-stats.tsx"
      via: "stats props 전달"
      pattern: "NursingSkillsStatsCard.*stats"
---

<objective>
반별 성적 비교 차트와 핵심간호술 별도 통계 섹션을 구현하여 /class-comparison 페이지에 통합한다.

Purpose: 교수가 A/B/C반의 평균, 최고점, 최저점을 시각적으로 비교하고, 핵심간호술(100점 만점 별도 평가)을 총점과 분리하여 독립적으로 추적할 수 있도록 한다.
Output: /class-comparison 라우트 페이지, 반별 비교 유틸, 핵심간호술 집계 유틸, 비교 차트 컴포넌트, 핵심간호술 카드 컴포넌트, 사이드바 링크
</objective>

<execution_context>
@C:/Users/김기훈.LAPTOP-1R73PMSJ/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/김기훈.LAPTOP-1R73PMSJ/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-student-lookup-class-comparison/04-RESEARCH.md
@.planning/phases/04-student-lookup-class-comparison/04-01-SUMMARY.md

# 기존 코드 참조
@types/grades.ts — StudentGrade, ClassCode 타입
@lib/grade-data.ts — getGradeData() 인터페이스
@lib/grade-kpi.ts — computeGradeKpi() classAverages 패턴
@components/dashboard/grade-histogram-chart.tsx — CLASS_COLORS, Recharts BarChart 패턴
@components/dashboard/grade-kpi-cards.tsx — KPI 카드 패턴 참조
@components/dashboard/mock-data-banner.tsx — MockDataBanner 재사용
@components/layout/sidebar.tsx — navItems 배열 (04-01에서 Users 추가됨)
</context>

<tasks>

<task type="auto">
  <name>Task 1: 반별 비교 유틸 + 핵심간호술 집계 유틸 + 차트/카드 컴포넌트 생성</name>
  <files>
    lib/grade-comparison.ts
    components/dashboard/class-comparison-chart.tsx
    components/dashboard/nursing-skills-stats.tsx
  </files>
  <action>
1. **lib/grade-comparison.ts** 신규 생성:
   - `ClassComparisonData` 인터페이스: `{ metric: string; A: number; B: number; C: number }` — Recharts BarChart grouped에 직접 사용
   - `buildClassComparisonData(students: StudentGrade[]): ClassComparisonData[]`:
     - 반별 통계 계산 내부 헬퍼 `classStats(cls: ClassCode)` — `{ avg, max, min }` 반환
     - 평균: `Math.round(sum / count * 10) / 10` (소수점 1자리)
     - 최고점/최저점: `reduce` 패턴 (빈 배열 안전 처리 — `scores[0]` 초기값)
     - 반환: `[{ metric: '평균', A, B, C }, { metric: '최고점', ... }, { metric: '최저점', ... }]`
   - `NursingSkillsStats` 인터페이스: `{ classCode: ClassCode | 'all'; average: number; highest: number; lowest: number; count: number }`
   - `computeNursingSkillsStats(students: StudentGrade[]): NursingSkillsStats[]`:
     - A반/B반/C반/전체 4개 그룹 각각 `nursingSkills` 필드만 집계 (`totalScore`와 독립)
     - 평균: `Math.round(sum / count * 10) / 10` (소수점 1자리)
     - 최고점/최저점: `reduce` 패턴 (빈 배열이면 `{ average: 0, highest: 0, lowest: 0, count: 0 }`)

2. **components/dashboard/class-comparison-chart.tsx** 신규 생성 (Client Component):
   - `"use client"` 선언
   - props: `{ data: ClassComparisonData[] }`
   - Recharts `BarChart` grouped (stackId 없이 Bar 3개 나란히)
   - XAxis: `dataKey="metric"` (평균/최고점/최저점)
   - YAxis: `domain={[0, 100]}`
   - Bar 3개: `dataKey="A"`, `dataKey="B"`, `dataKey="C"` — 각각 `CLASS_COLORS` 적용 (`A: 'hsl(220, 70%, 50%)'`, `B: 'hsl(160, 60%, 45%)'`, `C: 'hsl(30, 80%, 55%)'`)
   - Tooltip: `formatter={(value, name) => [`${value}점`, `${name}반`]}`
   - Legend: `formatter={(value) => `${value}반``
   - `ResponsiveContainer width="100%" height={350}`
   - Card 래퍼 + CardHeader("반별 성적 비교") + CardContent 구조

3. **components/dashboard/nursing-skills-stats.tsx** 신규 생성 (Server Component):
   - props: `{ stats: NursingSkillsStats[] }`
   - 제목: "핵심간호술 (별도 평가, 총점 미포함)" — 총점 미포함 명시
   - 4개 KPI 카드 (A반/B반/C반/전체) — 각각 평균, 최고점, 최저점, 학생 수 표시
   - Card 래퍼 사용, 반응형 그리드 `grid grid-cols-2 lg:grid-cols-4 gap-4`
   - 카드 구조: CardHeader(반 이름 + Stethoscope 아이콘(lucide-react)), CardContent(평균 큰 숫자 + 최고/최저 작은 텍스트)
   - 전체 카드는 테두리 강조 (`border-primary/50` 등)
   - 평균 점수는 `text-2xl font-bold`, 최고/최저는 `text-sm text-muted-foreground`
  </action>
  <verify>
    - `npx tsc --noEmit` — 타입 에러 없음
    - `lib/grade-comparison.ts`에서 `buildClassComparisonData`, `computeNursingSkillsStats`, `ClassComparisonData`, `NursingSkillsStats` export 확인
    - `class-comparison-chart.tsx`에 `"use client"`, `BarChart`, `Legend` 존재 확인
    - `nursing-skills-stats.tsx`에 "총점 미포함" 문구 존재 확인
  </verify>
  <done>
    - buildClassComparisonData()가 반별 평균/최고점/최저점 비교 데이터를 ClassComparisonData[]로 반환한다
    - computeNursingSkillsStats()가 nursingSkills 필드만 독립 집계하여 NursingSkillsStats[]를 반환한다
    - ClassComparisonChart가 A/B/C반 3개 Bar를 grouped로 나란히 표시한다
    - NursingSkillsStatsCard가 반별+전체 핵심간호술 평균/최고/최저를 카드로 표시한다
  </done>
</task>

<task type="auto">
  <name>Task 2: /class-comparison 페이지 라우트 + 사이드바 링크 + 빌드 검증</name>
  <files>
    app/(dashboard)/class-comparison/page.tsx
    components/layout/sidebar.tsx
  </files>
  <action>
1. **app/(dashboard)/class-comparison/page.tsx** 신규 생성 (Server Component):
   - `getGradeData()`로 학생 데이터 페칭
   - `buildClassComparisonData(students)`로 반별 비교 데이터 생성
   - `computeNursingSkillsStats(students)`로 핵심간호술 통계 계산
   - 페이지 구조:
     ```
     <div className="space-y-6">
       <h1 className="text-2xl font-bold">반별 비교</h1>
       <MockDataBanner dataSource={dataSource} />
       <ClassComparisonChart data={comparisonData} />
       <NursingSkillsStatsCard stats={nursingStats} />
     </div>
     ```
   - import: getGradeData (lib/grade-data), buildClassComparisonData/computeNursingSkillsStats (lib/grade-comparison), ClassComparisonChart, NursingSkillsStatsCard, MockDataBanner

2. **components/layout/sidebar.tsx** 수정:
   - lucide-react import에 `BarChart2` 추가 (04-01에서 `Users` 이미 추가됨)
   - navItems 배열에 학생 조회 다음으로 추가:
     ```
     { icon: BarChart2, label: '반별 비교', href: '/class-comparison' }
     ```
   - 최종 navItems 순서: 대시보드 → 학습성과 → 학생 조회 → 반별 비교

3. **빌드 검증:**
   - `npm run build` 실행하여 전체 빌드 성공 확인
   - `/class-comparison` 라우트가 빌드 출력에 포함되는지 확인
  </action>
  <verify>
    - `npm run build` — 빌드 성공, `/class-comparison` 라우트 포함
    - `app/(dashboard)/class-comparison/page.tsx`에서 getGradeData, buildClassComparisonData, computeNursingSkillsStats 호출 확인
    - sidebar.tsx navItems에 `/class-comparison` 링크 존재 확인
    - navItems 순서: 대시보드 → 학습성과 → 학생 조회 → 반별 비교
  </verify>
  <done>
    - /class-comparison 페이지가 반별 비교 차트와 핵심간호술 통계를 함께 표시한다
    - 사이드바에 "반별 비교" 링크가 "학생 조회" 다음에 표시되고 /class-comparison으로 이동한다
    - npm run build가 에러 없이 성공한다
  </done>
</task>

</tasks>

<verification>
1. `npm run build` 성공 — /class-comparison 라우트 포함
2. `npx tsc --noEmit` — 타입 에러 없음
3. 사이드바에 "반별 비교" 링크가 /students 다음에 표시
4. /class-comparison 페이지에서 반별 평균/최고점/최저점 grouped BarChart 표시
5. 핵심간호술 섹션이 총점과 분리되어 반별+전체 카드로 표시
6. 핵심간호술 섹션 제목에 "총점 미포함" 명시
</verification>

<success_criteria>
- 교수가 /class-comparison 페이지에서 A/B/C반의 평균, 최고점, 최저점을 BarChart로 나란히 비교할 수 있다
- 핵심간호술 통계가 총점과 독립된 별도 섹션에서 반별+전체로 표시된다
- 빌드가 성공하고 사이드바 탐색이 정상 동작한다
</success_criteria>

<output>
After completion, create `.planning/phases/04-student-lookup-class-comparison/04-02-SUMMARY.md`
</output>
